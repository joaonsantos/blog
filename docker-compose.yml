version: '3'

services:
  blog-nginx:
    image: blog-nginx
    container_name: blog-nginx
    ports:
      - "80:80"
    volumes:
      - web-root:/usr/share/nginx/html
      - certbot-etc:/etc/letsencrypt
      - certbot-var:/var/lib/letsencrypt
    networks:
      - app

  certbot:
    image: certbot/certbot
    container_name: certbot
    volumes:
      - certbot-etc:/etc/letsencrypt
      - certbot-var:/var/lib/letsencrypt
      - web-root:/usr/share/nginx/html
    networks:
      - app
    depends_on:
      - blog-nginx
    command: certonly --webroot --webroot-path=/usr/share/nginx/html --email joaopns05@gmail.com --agree-tos --no-eff-email --staging -d joaonsantos.dev  -d www.joaonsantos.dev

volumes:
  certbot-etc:
  certbot-var:
  web-root:

networks:
  app:
